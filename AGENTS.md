# Agent notes

Khaayaal marketing site. Built with TanStack Start (React) + Tailwind CSS.

- Routes live in `src/routes`.
- Shared UI primitives are in `src/components/ui`; brand components in `src/components/mojood`.
- Global styles and design tokens are in `src/styles.css`.
